.credit-registration
  .credit-registration__title
    %p.payment 支払い方法
    -# = form_for(@user, url: user_registration_path, id: 'charge-form', name: "inputForm") do |f|
    =render partial: "devise/registrations/credit"
