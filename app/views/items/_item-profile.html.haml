.item-profile
  = link_to item_path(item), class: 'item-profile__link' do
    %figure.item-profile__main
      %figcaption.item-profile__title
        %span.item-profile__title--text
          = item.name
      .item-profile__image
        %span.item-profile__image--price
          ¥
          = item.price
        = image_tag "#{item.images.first.name}", class: 'item-profile__image--item-image'